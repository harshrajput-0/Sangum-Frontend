export interface SettingsSection {
  id: string;
  label: string;
}

export interface GeneralInfo {
  name: string;
  slug: string;
  tagline: string;
  description: string;
}

export interface VisibilityOption {
  id: string;
  label: string;
  description: string;
}